[project]
name = "loan-prediction-system"
version = "0.1.0"
description = "Building the loan prediction system using ML!"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kagglehub>=1.0.1",
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "pyaml>=26.2.1",
    "pyspark>=4.1.2",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
